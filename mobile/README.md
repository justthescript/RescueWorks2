# RescueWorks Mobile

Expo based React Native app that talks to the same FastAPI backend.

It has a simple login screen and a pets list view that calls:

- POST /auth/token
- GET /pets

You can extend this to add adopter and foster specific flows, vet portal, tasks, and events.
