# RescueWorks Frontend

Simple React dashboard that connects to the RescueWorks FastAPI backend.

## Quick start

1. Install dependencies

   npm install

2. Run dev server

   npm run dev

The app expects the backend to run on http://localhost:8000
